// The colours a student can give an object. These are data, not theme: they are written into the
// saved file with the object, so they are the same in both themes and must never change meaning.
// They match the first colours the naming module hands out, so a picked colour looks like a given one.
export const SWATCHES = ['#4dabf7', '#ff6b6b', '#51cf66', '#fcc419', '#cc5de8', '#ff922b', '#22b8cf', '#f06595', '#e9ecef', '#adb5bd', '#63e6be', '#9775fa']
